# 🖼️ คู่มือใส่รูปภาพแทนอิโมจิ — ปั่นกับฟ่าง

สรุปทุกจุดที่ใส่ "รูปจริง" แทนอิโมจิได้ · ใส่รูปเมื่อไหร่ก็ขึ้นแทนทันที ถ้าไม่ใส่ก็ใช้อิโมจิ/แก้ว SVG เหมือนเดิม (ไม่พัง)

> ข้อมูลเมนูทั้งหมดอยู่ที่ `src/data/site.ts` · รูปวางในโฟลเดอร์ `public/...` · ใน path **ไม่ต้องมีคำว่า public** (เช่นไฟล์ `public/fresh/banana.png` → เขียน `image: "/fresh/banana.png"`)

---

## 📋 ตารางสรุป (ใส่รูปได้ทุกหัวข้อนี้)

| # | หัวข้อ | ใส่ที่ไหน (site.ts) | โฟลเดอร์ | แสดงจริง | **แนะนำอัปโหลด** | ไฟล์ |
|---|--------|--------------------|----------|----------|------------------|------|
| 1 | 🧋 **ท็อปปิ้ง** | `TOPPING_IMAGES` (คีย์ = ชื่ออังกฤษ) | `public/toppings/` | ~100×100 | **400×400** | JPG/PNG *(มี 46 รูปแล้ว)* |
| 2 | 🥝 **ตักสด ปั่นฟิน** (ผัก/ผลไม้/เพิ่มรสชาติ) | `FRESH_FRUITS` / `FRESH_VEGGIES` / `FRESH_FLAVORS` → ฟิลด์ `image` | `public/fresh/` | 24×24 | **96×96** | PNG พื้นโปร่ง |
| 3 | 🍹 **การ์ดหมวดเมนู** (หน้า /menu) | `MENU_SECTIONS` → ฟิลด์ `image` | `public/menu/cat/` | ~112×112 | **400×400** | PNG พื้นโปร่ง |
| 4 | ☕ **การ์ดเมนูเครื่องดื่ม** (กาแฟ/นม/ชา/สมูทตี้/โซดา/ร้อน) | `MENU_ITEMS` → ฟิลด์ `image` | `public/menu/` | ~280×176 | **800×800** | JPG/PNG/WebP |
| 5 | 🌿 **น้ำสมุนไพรโฮมเมด** | `HOMEMADE_BOTTLED` / `HOMEMADE_BLENDED` → ฟิลด์ `image` | `public/homemade/` | 48×48 | **200×200** | PNG พื้นโปร่ง |
| 6 | 🍪 **ขนม (Snacks)** | `SNACK_GROUPS[].items` → ฟิลด์ `image` | `public/snacks/` | ~280×160 | **800×600** | JPG/PNG/WebP |
| 7 | 🥪 **แซนด์วิช** | `SANDWICH_GROUPS[].items` → ฟิลด์ `image` | `public/snacks/` | ~280×160 | **800×600** | JPG/PNG/WebP |

*(โฟลเดอร์ `toppings`, `fresh`, `menu/cat`, `menu`, `homemade`, `snacks` สร้างไว้ให้แล้วครบ)*

---

## 📝 รายละเอียดแต่ละหัวข้อ

### 1) 🧋 ท็อปปิ้ง — 400×400 (จัตุรัส)
- แสดงในหน้า `/menu/toppings` และในกล่องเลือกท็อปปิ้งของทุกเมนู
- **มีรูปจริงครบ 46 รูปแล้ว** — ถ้าอยากเปลี่ยนรูปไหน แก้ path ใน `TOPPING_IMAGES` (คีย์เป็นชื่ออังกฤษ เช่น `"Golden Tapioca Pearls"`)
- อยากเพิ่มท็อปปิ้งใหม่พร้อมรูป: เพิ่มบรรทัดใน `TOPPING_IMAGES` เช่น
  `"Ice Cream": "/toppings/ice-cream.jpg",`

### 2) 🥝 ตักสด ปั่นฟิน — 96×96 (PNG โปร่ง)
- ช่องรูปในชิปเป็น **24×24** (คงที่ทุกช่อง) → อัปโหลด 96×96 เผื่อจอคมชัด
- มี 3 หมวด: `FRESH_FRUITS` (ผลไม้), `FRESH_VEGGIES` (ผัก), `FRESH_FLAVORS` (เพิ่มรสชาติ)
- ตัวอย่าง: `{ id: "banana", label: "กล้วย", en: "Banana", emoji: "🍌", image: "/fresh/banana.png" },`
- 💡 รูปในโถปั่น (แอนิเมชัน) จะใช้รูปนี้ด้วยอัตโนมัติถ้าใส่

### 3) 🍹 การ์ดหมวดเมนู (หน้า /menu) — 400×400 (PNG โปร่ง)
- การ์ดใหญ่ ๆ ในหน้ารวมเมนู (เมนูกาแฟ, เมนูนม, ...)
- ใส่ `image` ใน `MENU_SECTIONS` เช่น `image: "/menu/cat/coffee.png"` → รูปขึ้นแทนแก้ว SVG
- แนะนำวางในโฟลเดอร์ย่อย `public/menu/cat/` เพื่อไม่ปนกับรูปเมนูเครื่องดื่ม

### 4) ☕ การ์ดเมนูเครื่องดื่ม — 800×800 (จัตุรัสปลอดภัยสุด)
- การ์ดสินค้าจริงในหน้า `/menu/drinks`, `/menu/milk`, `/menu/tea`, `/menu/smoothie`, `/menu/soda`, `/menu/hot`
- ใส่ `image` ในแต่ละเมนูใน `MENU_ITEMS` เช่น `image: "/menu/grape-yogurt.jpg"` → รูปขึ้นแทนแก้ว SVG
- ถ้ารูปโหลดพลาดชั่วคราวจะ retry ให้เอง แล้ว fallback เป็นแก้ว SVG

### 5) 🌿 น้ำสมุนไพรโฮมเมด — 200×200 (PNG โปร่ง)
- รูป thumbnail เล็ก (48×48) ในหน้า `/menu/herbal`
- ใส่ `image` ใน `HOMEMADE_BOTTLED` และ `HOMEMADE_BLENDED` เช่น `image: "/homemade/chrysanthemum.png"`

### 6) 🍪 ขนม (Snacks) — 800×600
- การ์ดขนมในหน้า `/menu/snacks`
- ใส่ `image` ในแต่ละรายการใน `SNACK_GROUPS[].items` เช่น `image: "/snacks/cookie.jpg"`

### 7) 🥪 แซนด์วิช — 800×600
- ใช้การ์ดแบบเดียวกับขนม
- ใส่ `image` ในแต่ละรายการใน `SANDWICH_GROUPS[].items` เช่น `image: "/snacks/ham-sandwich.jpg"`

---

## ✅ กติกากลาง (ใช้กับทุกหัวข้อ)
- **ทรงสี่เหลี่ยมจัตุรัส** ปลอดภัยสุด (ทุกช่อง crop กลางภาพ) · วางตัวสินค้าไว้กลางภาพ
- ช่องเล็ก (ท็อปปิ้ง/ผัก-ผลไม้/สมุนไพร/หมวด) → **PNG พื้นหลังโปร่ง** สวยสุด
- การ์ดใหญ่ (เครื่องดื่ม/ขนม/แซนด์วิช) → JPG/PNG/WebP ก็ได้ (WebP ไฟล์เล็กสุด)
- ใส่ path **ขึ้นต้นด้วย `/`** และ **ไม่มีคำว่า public** เช่น `"/fresh/kiwi.png"`
- เปลี่ยนรูปเดิมที่ชื่อไฟล์เดิม แล้วเบราว์เซอร์ยังจำรูปเก่า → เปลี่ยนชื่อไฟล์ หรือเติม `?v=2` ท้าย path

## ⏳ ยังเป็นอิโมจิอย่างเดียว (ยังไม่มีช่องรูป — บอกได้ถ้าอยากให้เพิ่ม)
- ตัวเลือกใน **มิกซ์กับฟ่าง**: ฐาน (นม/โยเกิร์ต/โซดา/สมูทตี้/กาแฟ), ชนิดชา, ชนิดน้ำสมุนไพร, ไซรัป → ตอนนี้ใช้อิโมจิล้วน
- ถ้าอยากใส่สติกเกอร์ตรงนี้ด้วย บอกได้ เดี๋ยวเพิ่มช่อง `image` ให้เหมือนหัวข้ออื่น

---
*อยากให้ช่วยใส่รูปให้เลย ส่งไฟล์มาพร้อมบอกว่าเป็นของหัวข้อไหน/รายการอะไร ได้เลยครับ 💜*
